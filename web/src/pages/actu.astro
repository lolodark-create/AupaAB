---
// /actu was the original article-feed route. After the nav simplification
// (home page IS the feed now), this just redirects to / so bookmarks and
// external links keep working. 301 because this is a permanent move.
const params = Astro.url.searchParams.toString();
return Astro.redirect(params ? `/?${params}` : '/', 301);
---
